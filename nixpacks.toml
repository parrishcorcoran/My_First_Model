# Tell Railway/Nixpacks this is a Python project (the presence of /web
# would otherwise make it auto-detect Node).
providers = ["python"]

[phases.install]
cmds = ["pip install -r api/requirements.txt"]

[start]
cmd = "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
